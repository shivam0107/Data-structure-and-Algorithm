#include <bits/stdc++.h>
using namespace std;
    
 
 
 
 
int main()
{

    // int first = 0;
    // int second = 10;
    // int *ptr = &second;
    // *ptr = 9;

    // cout << first << " " << second << endl;
     // 0 9

    // int first = 6;
    // int *p = &first;
    // int *q = p;                   7 ans
    // (*q)++;
    // cout << first << endl;

    // int first = 8;
    // int *p = &first;              8 9 ans
    // cout << (*p)++ << " ";
    // cout << first << endl;

    // int *p = 0;
    // int first = 110;
    // *p = first;           it should be *p = &first 
    //                       otherwise it will through seg fault        
    // cout << *p << endl;

    // int first = 8;
    // int second = 11;
    // int *third = &second;         11 13 ans
    // first = *third;
    // *third = *third + 2;
    // cout << first << " " << second << endl;

    // float f = 12.5;
    // float p  = 21.5;
    // float *ptr = &f;    // 21.5 21.5 21.5 ans
    // (*ptr)++;
    // *ptr = p;
    // cout << *ptr << " " << f << " " << p << endl;

    // int arr[5];
    // int *ptr;          // 20 4 ans
    // cout << sizeof(arr) << " " << sizeof(ptr) << endl;

    

    return 0;
}